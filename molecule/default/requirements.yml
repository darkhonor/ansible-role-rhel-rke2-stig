---
###############################################################################
# Filename: molecule/default/requirements.yml
# Role: ansible-role-rhel-rke2-stig
# Summary: Ansible Galaxy requirements for Molecule testing
# Last Updated: 23 Jan 2026
# Classification: UNCLASSIFIED
###############################################################################
# Exact-pinned for reproducible CI builds (Galaxy collections cannot be
# SHA-pinned; exact version is the integrity ceiling). The published
# /requirements.yml intentionally keeps >= ranges for downstream consumers.
collections:
  - name: ansible.posix
    version: "2.2.0"
  - name: community.general
    version: "13.0.1"
